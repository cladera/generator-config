language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-config' ]; then cd .. && eval "mv $currentfolder generator-config" && cd generator-config; fi

